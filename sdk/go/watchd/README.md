# Go SDK

Future client library for keeping a local projection synchronized with a watch source.

The SDK will persist its cursor, apply only complete transactions, and atomically replace its projection during a resync.

